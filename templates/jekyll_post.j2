---
layout: post
title: "AI 기술 다이제스트 - {{ date }}"
date: {{ date }}
categories: [AI, Tech]
tags: [AI, LLM, 트렌드, 기술블로그]
---

> 🤖 AI 기술 트렌드를 자동으로 수집하고 요약한 다이제스트입니다.

---

{% for source in sources %}
## {{ source.emoji }} {{ source.name }}

{% for item in source["items"] %}
<div class="digest-item" markdown="1">

<h3><a href="{{ item.url }}" target="_blank">{{ item.title }}</a></h3>

{% if item.categories %}
<div class="categories">
{% for cat in item.categories %}<span class="category-tag">{{ cat.emoji }} {{ cat.name }}</span> {% endfor %}
</div>
{% endif %}

{{ item.summary }}

{% if item.author %}<small>👤 {{ item.author }}</small>{% endif %}

</div>

{% endfor %}

---

{% endfor %}

## 📊 오늘의 키워드

<div class="keywords">
{% for keyword in top_keywords %}<code>{{ keyword }}</code> {% endfor %}
</div>

---

<small>🤖 이 포스트는 <a href="https://github.com/joyuno/ai-tech-digest">AI Tech Digest</a>에 의해 자동 생성되었습니다.</small>
